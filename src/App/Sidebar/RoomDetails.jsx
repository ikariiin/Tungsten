import React, { Component } from 'react';
import Window from "./Window";

export default class RoomDetails extends Component {
    render() {
        return (
            <Window customStyles={ {} } theme='#F5F5F5'>
            </Window>
        )
    }
}